# ==========================================
# Nostr relay (AAE interface infrastructure)
# ==========================================
FROM scsibug/nostr-rs-relay:0.10.0

EXPOSE 8080

# Persist SQLite outside the container:
#   -v ./data:/usr/src/app/db
# Optional config:
#   -v ./config.toml:/usr/src/app/config.toml:ro
